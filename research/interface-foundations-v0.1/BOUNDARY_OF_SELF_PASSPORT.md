# Passport IF-BS-01

Date: 2026-07-31
Object: the boundary condition for a proper identity in a connected world.

## Claim levels

1. In a connected topological space, an empty-boundary region is empty or whole.
   Status: standard topology.
2. A nonempty proper region in a connected space has nonempty boundary.
   Status: standard topology.
3. A concrete Kuratowski closure topology defines interior and frontier.
   The connected-space boundary theorem is derived in Lean without accepting
   the connected-boundary implication as an input contract.
4. A SelfRecord contains at least one internal and one external witness.
   Status: checked in Lean.
5. The internal point p is physical consciousness.
   Status: author interpretation; not a mathematical or neuroscientific theorem.
6. The resolution region X_eps(a) = {b | eps < D(a,b)} is proper when
   D(a,a) = 0 and a distinguishable witness exists above eps.
   Status: checked in Lean.
7. In a connected closure topology that resolved region has nonempty frontier.
   Status: checked in Lean.

## Public position

Place this scene after IF-FD-01 and before IF-GF-01:

\[
\text{question}
\to
\text{first distinction}
\to
\text{boundary of self}
\to
\text{field and memory}
\to
\text{model-external monopole}.
\]

## Next slice

Instantiate D and the closure topology in a finite computable chamber. Produce
the above-threshold witness from chamber data rather than accepting it as an
assumption, then compare the computed frontier with PlanckTouch_epsilon.
