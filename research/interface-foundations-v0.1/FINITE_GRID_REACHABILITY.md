# IF-BS-18: finite unit-grid reachability

## Exact graph relation

`GridReachable(P,p,q)` is generated by unit-adjacent grid edges whose endpoints satisfy `P`, together with reflexivity and finite transitive composition. It is therefore a literal finite-path relation in the sampled grid, not a geometric shorthand.

## Segment realization

Every certified horizontal or vertical integer segment is recursively decomposed into unit-adjacent edges. The recursion decreases the remaining coordinate distance, and the segment predicate supplies membership at every new vertex.

## Two connected subgraphs

IF-BS-16 now yields an actual inside-grid path from every inside sample to the centre. IF-BS-17 yields an actual outside-grid path from every outside sample to the common frame corner. Reversing and composing these paths proves pairwise reachability within each region.

## Verification

The main module and independent audit compile with Lean 4.32.1, and the source-gap scan is empty. The global pairwise-connectivity theorems report only `propext`, `Classical.choice`, and `Quot.sound`; no project-specific axiom is introduced.

## Red boundary

The sampled inside and outside subgraphs are now formally connected in exactly the graph used to define threshold crossings. The remaining theorem is finite planar duality: the cut between two connected complementary regions in the rectangular grid induces one medial contour cycle.

## Next

IF-BS-19 should prove the rectangular-grid cut/medial-cycle theorem and apply it to the concrete IF-BS-15 traversal.
