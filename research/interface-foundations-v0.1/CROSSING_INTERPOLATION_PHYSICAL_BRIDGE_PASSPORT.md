# Passport: IF-BS-22F-B3C2B2B

## Objective

Identify the exact crossing interpolation with the endpoint representation used by the real local contour segment.

## Closed obligations

- Physical x and y interpolation formulas.
- Exact equality with `firstRealEndpoint edge edge`.
- Exact equality with `segmentRealPoint 0 segment vertex vertex`.
- Unit-interval bounds for the edge interpolation parameter.

## Red boundary

The branch-specific metric estimate has not yet been assembled into `ReverseCoverageWitness`.

## Next point

IF-BS-22F-B3C2C: instantiate the `3/m` dominant-coordinate estimate for R/L/T/B, combine it with the `<1/m` orthogonal error, prove Euclidean distance at most `sqrt(10)/m` or the clean public bound `4/m`, and construct reverse coverage for every target point.
