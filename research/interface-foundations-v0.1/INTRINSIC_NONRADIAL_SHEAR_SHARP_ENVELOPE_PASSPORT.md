# Passport: IF-BS-22F-F8C17

## Node

Sharp spectral optimization of the exact realizable directional diamond.

## Verified output

- The exact minimum and maximum of `Phi_a` on `D` are `lambda_-(a)` and `lambda_+(a)`.
- Explicit normalized lower and upper witnesses attain both edges.
- The reciprocal observable has exact extrema `lambda_+^(-1)` and `lambda_-^(-1)`.
- The sharp inverse regularity modulus is never worse than the F8C16 certified modulus.
- At `a=1/2`, the improvement is strict.
- For every positive `delta`, a finite exact sample exists with `0 <= Gamma_delta <= L_sharp(a) delta`.
- The carrier and audit contain no `axiom`, `sorry`, or `admit`.

## Red boundary

The forward Lipschitz numerator remains a proved but non-optimal estimate. F8C17 sharpens the spectral denominator only.

## Next passport step

`IF-BS-22F-F8C18`: optimize the exact Lipschitz modulus of `Phi_a` and its reciprocal on `D`, replacing the remaining coarse factor `L_2(a)`.
