# Passport: IF-BS-22F-F8C13

## Node

`Inverse directional blow-up / exact relaxed poles / finite gap`

## Input

- F8C12 compact chamber and exact forward squared-stretch polynomial.
- F8C12 explicit forward regularity.
- F8C11 compact finite-net infrastructure.

## Verified output

- Admissible range `0 <= a` and `sqrt(2)a < 1`.
- Exact relaxed bounds `(1-sqrt(2)a)^2` and `(1+sqrt(2)a)^2`.
- Explicit lower and upper pole witnesses in the chamber.
- Reciprocal observable with exact two-sided bounds.
- Reciprocal regularity `L_inv(a)`.
- Finite exact inverse sample with global bound.
- Tightness-gap theorem `0 <= Gamma_delta <= L_inv(a)delta`.

## Red boundary

The relaxed poles need not both be finite-chord realizable. The exact closure of the chord blow-up image is still unknown.

## Next passport step

`IF-BS-22F-F8C14`: characterize the closure of realizable directional records, decide which relaxed poles are approachable, and replace the safe envelope by the exact intrinsic chord envelope where possible.
