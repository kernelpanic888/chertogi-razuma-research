# Passport: IF-BS-22F-F8C20

## Node

Explicit asymptotic circle witnesses, least coefficient one, and their exact cancellation after lifting to the directional diamond.

## Verified output

- `P_t` and `Q_t` are explicit normalized symmetric unit-circle points.
- Their squared coordinate difference is `2t/(1+t^2)`.
- Their squared distance is `4t^2/(1+t^2)`.
- Every nonnegative constant below `1` is defeated by a finite witness parameter.
- The constant `1` is the least global coefficient for `|u_x^2-v_x^2|`.
- Equality is not attained by distinct points, so sharpness occurs only as a supremum.
- The positive-edge lifts lie in the directional diamond for `0<=t<=1`.
- Their slope coordinates are equal and their lifted distance is unchanged.
- Their complete forward difference is exactly `-4at/(1+t^2)`.
- The carrier and audit contain no local `axiom`, `sorry`, or `admit`.

## Red boundary

The sharp isolated circle family cancels inside the complete chamber. It does not determine the least full pairwise Lipschitz constant.

## Next passport step

`IF-BS-22F-F8C21`: parameterize admissible tangent directions of the complete directional diamond and optimize the local derivative of `Phi_a` in the max product metric.
