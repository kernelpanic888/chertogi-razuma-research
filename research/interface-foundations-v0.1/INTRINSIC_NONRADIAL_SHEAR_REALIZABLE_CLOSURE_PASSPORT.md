# Passport: IF-BS-22F-F8C15

## Node

`Exact closure of realizable directional blow-ups`

## Verified output

- The directional width `w(u)=|u_x|+|u_y|` is positive for every unit direction.
- The finite endpoint margin is `m_t(u)=w(u)-t|u_x||u_y|`.
- The chord-slope family is continuous in its base point.
- Its two finite endpoint values are exactly `+m_t(u)` and `-m_t(u)`.
- Every strict interior record `|s|<w(u)` is realized by an actual finite chord.
- Every boundary record is a limit of strict interior records.
- The directional diamond is contained in the realizable closure.
- Together with F8C14, `closure(records)=D` exactly.

## Red boundary

The exact chamber is known, but the earlier forward and inverse mesh certificates still use the relaxed chamber constants. Their exact realizable constants and certificate gaps have not yet been transported and compared.

## Next passport step

`IF-BS-22F-F8C16`: transport finite forward/inverse certificates from the relaxed strip to the exact realizable diamond and derive the improved mesh-gap constants.
