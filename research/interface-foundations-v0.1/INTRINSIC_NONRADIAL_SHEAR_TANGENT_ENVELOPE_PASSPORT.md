# Passport: IF-BS-22F-F8C21

## Node

Exact tangent differential, exact pointwise dual norm, and exact compact local envelope on the directional diamond.

## Verified output

- The raw tangent differential reduces to `2a(x s theta+(y+a s)sigma)`.
- The unit max-box dual norm is exactly `2a(|x s|+|y+a s|)`.
- The pointwise value is attained by explicit sign velocities.
- Absolute coordinates reduce every diamond point to the positive quarter circle.
- The positive boundary record `(X,Y,X+Y)` attains the reduced scalar density.
- The scalar density has an attained maximum by compactness and continuity.
- `L_tan(a)=2aE(a)` is the greatest local tangent value over the complete chamber.
- At `a=0`, the local tangent modulus is exactly zero.
- The carrier and audit contain no local `axiom`, `sorry`, or `admit`.

## Red boundary

The exact local modulus is not yet transported to a global pairwise bound in the ambient max chord metric.

## Next passport step

`IF-BS-22F-F8C22`: derive the scalar stationary equation, prove uniqueness of its maximizer, certify the half-amplitude value, and isolate the remaining path-to-chord bridge.
