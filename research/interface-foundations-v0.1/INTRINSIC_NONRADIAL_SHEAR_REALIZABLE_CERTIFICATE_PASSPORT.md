# Passport: IF-BS-22F-F8C16

## Node

`Exact-domain forward/inverse finite certificates`

## Verified output

- Every point of the directional diamond belongs to the old relaxed chamber.
- On the diamond, `(1-a)^2/2 <= forwardBlowUpSq(a,point)` for `0<=a<1`.
- The certified lower square is the maximum of the relaxed and diamond bounds.
- The reciprocal observable is bounded by the inverse certified lower square.
- Its regularity modulus is `L_2/certifiedLower^2`.
- The new inverse regularity modulus never exceeds the relaxed modulus.
- The new inverse mesh term never exceeds the relaxed mesh term for `delta>=0`.
- At `a=1/2` and `delta>0`, the improvement is strict.
- Compactness produces finite forward and inverse delta-net certificates directly on the exact realizable diamond.

## Red boundary

The hybrid lower bound is certified but not proved sharp. Exact minimizers of `Phi_a` on the directional diamond and the optimal forward/inverse regularity constants remain open.

## Next passport step

`IF-BS-22F-F8C17`: solve the sharp spectral optimization of `Phi_a` on the directional diamond, identify extremizing directions and replace the hybrid lower certificate by the exact envelope.
