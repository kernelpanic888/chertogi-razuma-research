# Passport: IF-BS-22F-F8C19

## Node

Unit-circle coupling, strict non-attainment and transfer to the inverse finite certificate.

## Verified output

- `|u_x^2-v_x^2| <= d(u,v)` for all directions on the unit circle.
- The inequality is strict for every distinct pair.
- The same strict spatial estimate holds in the full blow-up chamber, including pairs differing only in slope.
- `L_circle(a)=1+2(1+a)R(a)` is a proved global forward modulus.
- Every distinct pair satisfies a strict final forward bound.
- The reciprocal modulus and every positive-radius finite mesh term are strictly smaller than F8C18.
- At `a=1/2`, the numerator is `1+3sqrt(5/2) ~= 5.7434`.
- The carrier and audit contain no local `axiom`, `sorry`, or `admit`.

## Red boundary

Strict pointwise non-attainment does not determine the supremum. The sharpness of coefficient one and the least complete pairwise modulus remain open.

## Next passport step

`IF-BS-22F-F8C20`: construct an explicit asymptotic circle witness sequence, prove that the coefficient-one bound is sharp as a supremum, and test whether the slope component can align with that sequence.
